const app = Vue.createApp({
    data() {
        return {
            product: {
                name: 'Socks',
                description: 'Is just comfortable socks with a Vue logo in it'
            }
        }
    }
});